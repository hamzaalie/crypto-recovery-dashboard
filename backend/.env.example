# Application
NODE_ENV=development
PORT=3000

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Use ONE of the following options:

# Option 1: DATABASE_URL (recommended for online PostgreSQL)
# Get your FREE connection string from:
# - Neon (https://neon.tech) - Recommended, generous free tier
# - Supabase (https://supabase.com)
# - ElephantSQL (https://elephantsql.com)
# - Railway (https://railway.app)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Option 2: Individual config (for local PostgreSQL)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=postgres
# DB_PASSWORD=postgres
# DB_DATABASE=crypto_recovery
# DB_SSL=false

# ===========================================
# JWT CONFIGURATION
# ===========================================
# 🔐 SECURITY CRITICAL: Must be set before starting the application
# Generate a secure random string with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ⚠️ Application will NOT start without JWT_SECRET set
# ⚠️ NEVER use the example value below in production
JWT_SECRET=
JWT_EXPIRES_IN=7d

# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@cryptorecovery.com

# ===========================================
# FILE UPLOAD
# ===========================================
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# ===========================================
# FRONTEND URL (for CORS)
# ===========================================
FRONTEND_URL=http://localhost:5173
